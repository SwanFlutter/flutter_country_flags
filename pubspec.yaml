name: flutter_country_flags
description: "A comprehensive Flutter package for displaying country flags with support for multiple input formats and customizable styling options."
version: 0.0.1
homepage: "https://github.com/SwanFlutter/flutter_country_flags"

environment:
  sdk: ^3.8.0
  flutter: ">=1.17.0"

dependencies:
  flutter:
    sdk: flutter

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0


flutter:


  assets:
    - assets/
 
